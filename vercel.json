{
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api"
        }
    ],
    "outputDirectory": "dist/public",
    "functions": {
        "api/index.ts": {
            "includeFiles": "server/**,shared/**"
        }
    }
}